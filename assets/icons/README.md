# Icons

Inline SVG only, kept small and hand-authored — no icon library. The one icon currently in use (`public/icons/app-icon.svg`, the PWA/app icon) lives in `public/icons` since it needs a stable URL for the manifest; UI icons referenced from screens should go here as this data category grows.
